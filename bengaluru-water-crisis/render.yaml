services:
  - type: web
    name: neeru-nadi-water-crisis
    env: python
    plan: free
    buildCommand: "bash build.sh"
    startCommand: "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
